CREATE DATABASE java_fx_project;

-- Products Table
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    price NUMERIC(10,2) NOT NULL,
    category VARCHAR(100),
    stock INTEGER NOT NULL
);

-- Customers Table
CREATE TABLE customers (
    id SERIAL PRIMARY KEY,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    phone VARCHAR(20)
);

-- Orders Table
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    customer_id INTEGER REFERENCES customers(id),
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(20) NOT NULL,
    total_amount NUMERIC(10,2) NOT NULL
);

-- Order Items Table (for many-to-many relationship)
CREATE TABLE order_items (
    id SERIAL PRIMARY KEY,
    order_id INTEGER REFERENCES orders(id),
    product_id INTEGER REFERENCES products(id),
    quantity INTEGER NOT NULL,
    price NUMERIC(10,2) NOT NULL
);

-- Invoices Table
CREATE TABLE invoices (
    id SERIAL PRIMARY KEY,
    order_id INTEGER UNIQUE REFERENCES orders(id),
    invoice_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    total_amount NUMERIC(10,2) NOT NULL
);

-- Add a nullable status to products for inventory management
ALTER TABLE products ADD COLUMN status VARCHAR(20) DEFAULT 'AVAILABLE';

-- Add some constraints and indexes
ALTER TABLE customers ADD CONSTRAINT unique_email UNIQUE (email);
CREATE INDEX idx_order_customer ON orders(customer_id);
CREATE INDEX idx_order_status ON orders(status);

INSERT INTO products (name, price, category, stock)
VALUES
('Laptop', 999.99, 'Electronics', 50),
('Smartphone', 599.99, 'Electronics', 150),
('Headphones', 199.99, 'Accessories', 200),
('Keyboard', 49.99, 'Accessories', 300);

-- Insert customers
INSERT INTO customers (first_name, last_name, email, phone)
VALUES
('melina', 'maouchi', 'melinamaouchi@gmail.com', '123-456-7890'),
('Jane', 'yefsah', 'jane.yefsah@gmail.com', '234-567-8901'),
('vanessa', 'fadili', 'vanessa.fadili@gmail.com', '345-678-9012'),
('william', 'sail', 'william.sail@gmail.com', '456-789-0123');

-- Insert orders

INSERT INTO orders (customer_id, order_date, status, total_amount)
VALUES
(1, '2025-01-15 10:30:00', 'Pending', 1599.97),
(2, '2025-01-20 14:45:00', 'Shipped', 1199.98),
(3, '2025-02-01 09:00:00', 'Delivered', 449.97),
(4, '2025-02-05 16:20:00', 'Cancelled', 1499.98);


-- Insert order items
INSERT INTO order_items (order_id, product_id, quantity, price)
VALUES
(1, 1, 1, 999.99),
(1, 2, 1, 599.99),
(2, 2, 2, 599.99),
(3, 3, 1, 199.99),
(3, 4, 1, 49.99);

-- Insert invoices
INSERT INTO invoices (order_id, total_amount)
VALUES
(1, 1599.97),
(2, 1199.98),
(3, 449.97),
(4, 1499.98);